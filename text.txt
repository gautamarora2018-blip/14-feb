<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
  <title>Be My Valentine 💖</title>

  <link href="https://fonts.googleapis.com/css2?family=Dancing+Script:wght@600;700&family=Poppins:wght@300;400;600&display=swap" rel="stylesheet">

  <style>
    * {
      box-sizing: border-box;
      margin: 0;
      padding: 0;
    }

    body {
      height: 100vh;
      font-family: 'Poppins', sans-serif;
      background: linear-gradient(180deg, #ffd6e8, #f48fb1);
      overflow: hidden;
      display: flex;
      align-items: center;
      justify-content: center;
      text-align: center;
      color: #8b004f;
    }

    .container {
      z-index: 5;
      padding: 20px;
      max-width: 500px;
      width: 100%;
    }

    .bear {
      font-size: 64px;
      animation: bounce 1.5s infinite ease-in-out;
      margin-bottom: 10px;
    }

    @keyframes bounce {
      0%, 100% { transform: translateY(0); }
      50% { transform: translateY(-20px); }
    }

    h1 {
      font-family: 'Dancing Script', cursive;
      font-size: 42px;
      margin-bottom: 10px;
    }

    .subtitle {
      font-style: italic;
      font-size: 16px;
      margin-bottom: 25px;
    }

    .buttons {
      display: flex;
      justify-content: center;
      gap: 30px;
      margin-bottom: 25px;
      position: relative;
    }

    button {
      border: none;
      cursor: pointer;
      font-size: 18px;
      padding: 14px 28px;
      border-radius: 999px;
      transition: transform 0.2s, box-shadow 0.2s;
    }

    #yesBtn {
      background: linear-gradient(135deg, #ff1744, #ff5252);
      color: white;
      font-size: 20px;
      box-shadow: 0 0 15px rgba(255, 23, 68, 0.5);
    }

    #yesBtn:hover {
      transform: scale(1.08);
      box-shadow: 0 0 25px rgba(255, 23, 68, 0.9);
    }

    #noBtn {
      background: white;
      color: #ff1744;
      border: 2px solid #ff1744;
      position: fixed;
    }

    .closing {
      font-size: 14px;
      margin-top: 10px;
    }

    .heart {
      position: fixed;
      bottom: -40px;
      font-size: 24px;
      animation: floatUp linear infinite;
      opacity: 0;
      pointer-events: none;
    }

    @keyframes floatUp {
      0% { transform: translateY(0); opacity: 0; }
      10% { opacity: 1; }
      90% { opacity: 1; }
      100% { transform: translateY(-110vh); opacity: 0; }
    }

    .celebration {
      position: fixed;
      inset: 0;
      background: linear-gradient(180deg, #ffd6e8, #f06292);
      display: none;
      align-items: center;
      justify-content: center;
      flex-direction: column;
      z-index: 20;
      color: #fff;
      padding: 20px;
    }

    .celebration h2 {
      font-size: 52px;
      animation: pulse 1.2s infinite;
    }

    @keyframes pulse {
      0%, 100% { transform: scale(1); }
      50% { transform: scale(1.15); }
    }

    .spin {
      font-size: 70px;
      margin: 20px;
      animation: spin 2s linear infinite;
    }

    @keyframes spin {
      to { transform: rotate(360deg); }
    }

    .confetti {
      position: fixed;
      width: 10px;
      height: 10px;
      top: 50%;
      left: 50%;
      opacity: 0.9;
      animation: explode 3s forwards ease-out;
    }

    @keyframes explode {
      to {
        transform: translate(var(--x), var(--y)) rotate(720deg);
        opacity: 0;
      }
    }
  </style>
</head>
<body>

  <div class="container" id="question">
    <div class="bear">🐻</div>

    <!-- CHANGE NAME HERE -->
    <h1>Will You Be My Valentine, Alex? 💖</h1>

    <div class="subtitle">
      Because every moment with you feels special ✨
    </div>

    <div class="buttons">
      <button id="yesBtn">Yes 💘</button>
      <button id="noBtn">No</button>
    </div>

    <div class="closing">
      Every love story is beautiful, but ours is my favourite 💕
    </div>
  </div>

  <div class="celebration" id="celebration">
    <h2>YAYYYY!! 🎉</h2>
    <div class="spin">💃</div>
    <p>You just made my heart so happy 💖</p>
  </div>

  <script>
    let heartInterval;

    function startHearts() {
      heartInterval = setInterval(() => {
        const heart = document.createElement("div");
        heart.className = "heart";
        heart.textContent = "❤️";
        heart.style.left = Math.random() * 100 + "vw";
        heart.style.fontSize = 16 + Math.random() * 30 + "px";
        heart.style.animationDuration = 6 + Math.random() * 6 + "s";
        document.body.appendChild(heart);
        setTimeout(() => heart.remove(), 12000);
      }, 600);
    }
    startHearts();

    const noBtn = document.getElementById("noBtn");
    const yesBtn = document.getElementById("yesBtn");

    const messages = [
      "Nope 😜",
      "Try again 💕",
      "Are you sure? 😏",
      "Not allowed 😌",
      "Think again 💖"
    ];

    let msgIndex = 0;

    function moveNoButton() {
      const yesRect = yesBtn.getBoundingClientRect();
      let x, y;

      do {
        x = Math.random() * (window.innerWidth - noBtn.offsetWidth - 20);
        y = Math.random() * (window.innerHeight - noBtn.offsetHeight - 20);
      } while (
        x > yesRect.left - 40 &&
        x < yesRect.right + 40 &&
        y > yesRect.top - 40 &&
        y < yesRect.bottom + 40
      );

      noBtn.style.left = x + "px";
      noBtn.style.top = y + "px";
      noBtn.textContent = messages[msgIndex];
      msgIndex = (msgIndex + 1) % messages.length;
    }

    noBtn.addEventListener("click", moveNoButton);
    noBtn.addEventListener("touchstart", moveNoButton);

    yesBtn.addEventListener("click", () => {
      document.getElementById("question").style.display = "none";
      document.getElementById("celebration").style.display = "flex";
      clearInterval(heartInterval);
      document.querySelectorAll(".heart").forEach(h => h.remove());

      for (let i = 0; i < 120; i++) {
        const confetti = document.createElement("div");
        confetti.className = "confetti";
        confetti.style.background = `hsl(${Math.random() * 360}, 100%, 60%)`;
        confetti.style.setProperty("--x", `${(Math.random() - 0.5) * 600}px`);
        confetti.style.setProperty("--y", `${(Math.random() - 0.5) * 600}px`);
        document.body.appendChild(confetti);
        setTimeout(() => confetti.remove(), 3000);
      }
    });
  </script>

</body>
</html>